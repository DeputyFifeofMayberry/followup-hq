# Copy to `.env` or `.env.local` and fill in values from Supabase (Settings → API)
# or pull from Vercel: `npx vercel link` then `npx vercel env pull .env.local`
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
